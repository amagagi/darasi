<?php

use Illuminate\Support\Facades\Route;

Route::get('/', function () {
    return 'DARASI API - Serveur fonctionne!';
});

// Route de test simple SANS CONTROLLER
Route::post('/api/login', function () {
    return response()->json([
        'message' => 'Login endpoint fonctionne',
        'email' => request('email')
    ]);
});